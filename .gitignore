# Ignorar a pasta node_modules
node_modules/

# Ignorar arquivos de log
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignorar arquivos de cache
.DS_Store # macOS
Thumbs.db # Windows

# Ignorar variáveis de ambiente (se você tiver .env na raiz)
.env

# Ignorar builds do frontend (se você tiver uma pasta 'build' ou 'dist' dentro de 'frontend')
# Ex: se o build do React estiver em frontend/build
frontend/build/
frontend/dist/

# Ignorar builds do backend (se você tiver uma pasta 'build' ou 'dist' dentro de 'backend')
backend/build/
backend/dist/

# Ignorar arquivos temporários ou de configuração local
*.tmp
*.swp
*.swo